/**
 * BaseN-encodes a Uint8Array using the given alphabet.
 *
 * @param {Uint8Array} input the bytes to encode in a Uint8Array.
 * @param {string} alphabet custom alphabet.
 *
 * @return {string} the baseN-encoded output string.
 */
export declare function encode(input: Uint8Array, alphabet?: string): string;
/**
 * Decodes a baseN-encoded (using the given alphabet) string to a
 * Uint8Array.
 *
 * @param {string} input the baseN-encoded input string.
 * @param {string} alphabet custom alphabet.
 *
 * @return {Uint8Array} the decoded bytes in a Uint8Array.
 */
export declare function decode(input: string, alphabet?: string): Uint8Array;
//# sourceMappingURL=baseN.d.ts.map